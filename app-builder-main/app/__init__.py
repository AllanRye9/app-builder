"""apk-builder — FastAPI backend.

Turns an uploaded React/Capacitor (or native Android) project archive into
a downloadable debug APK. This package is a line-for-line-equivalent port
of the original Express/Node backend (see the project README), kept
API-compatible with the existing React dashboard in ``web/``.
"""
