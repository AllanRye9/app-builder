{
  "name": "apk-builder",
  "version": "2.0.0",
  "description": "Web service that turns an uploaded React/Capacitor project into a downloadable APK using an isolated Docker build.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build:web": "npm --prefix web install && npm --prefix web run build",
    "dev:web": "npm --prefix web run dev"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "express": "^4.21.2",
    "multer": "^2.0.1",
    "uuid": "^11.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
